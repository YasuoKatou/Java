<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="yks.ticket.lite.dao.TicketKindDao">
  <resultMap id="ticketKindResultMap" type="yks.ticket.lite.entity.TicketKindEntity" autoMapping="true">
  </resultMap>

  <select id="findByProject" resultMap="ticketKindResultMap">
    select
        T1.project_id  --プロジェクトID
      , T1.id          --進捗ID
      , T1.disp_seq    --表示順
      , T1.name        --進捗名称
      , T1.versionNo   --バージョンNo
    from t_ticket_kind T1
    where T1.project_id = #{project_id}
      and T1.available = 'yes'
    order by T1.disp_seq
  </select>
</mapper>